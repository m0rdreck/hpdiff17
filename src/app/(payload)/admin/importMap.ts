/**
 * Carte des composants personnalisés du back-office.
 * Régénérée par `pnpm payload generate:importmap` — laisser vide tant
 * qu'aucun composant custom n'est déclaré dans payload.config.ts.
 */
export const importMap = {};
